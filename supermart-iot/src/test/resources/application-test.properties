spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=false
spring.jpa.defer-datasource-initialization=true
app.telemetry.rate-limit-per-minute=10
logging.level.com.supermart=WARN

# ─── JWT (test values — never use real secrets in test properties) ─────────────
# SCRUM-63: Access token expiration updated to 60 minutes (3600000 ms).
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.access-token-expiration-ms=3600000
app.jwt.refresh-token-expiration-ms=86400000

# ─── CORS (test value — localhost only) ───────────────────────────────────────
app.cors.allowed-origins=http://localhost:3000
